apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: it-helpdesk
spec:
  storageClassName: it-helpdesk-storage
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteOnce
