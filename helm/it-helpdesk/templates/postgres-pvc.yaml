apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: it-helpdesk-storage
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteOnce