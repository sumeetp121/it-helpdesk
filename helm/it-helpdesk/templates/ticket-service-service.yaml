apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: ticket-service

  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002

  type: ClusterIP