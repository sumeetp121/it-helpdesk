apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: ticket-service

  ports:
    - protocol: TCP
      port: {{ .Values.service.ticketService.port }}
      targetPort: {{ .Values.service.ticketService.port }}

  type: ClusterIP